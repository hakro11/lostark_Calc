import { userInfo } from '../interface/interface';
import { classInformation } from './../constants/classInfo';


export function damCalc(root_Class: userInfo[][]) {
    let attackDam
    const damage = attackDam
}

function catchClass(userCharInfo: any, classInformation: userInfo[]) {
    classInformation.filter(i => i.className == "")
}